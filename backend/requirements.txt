# =========================================================================
# TheraAI Backend - Python Dependencies
# Python 3.11+ | FastAPI + Motor + HuggingFace + Ollama
#
# GPU install (default - RTX 3070 8GB or any CUDA 12.x GPU):
#   pip install -r requirements.txt
#
# CPU-only install (no NVIDIA GPU):
#   Step 1: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   Step 2: pip install -r requirements.txt
#   pip will skip re-installing torch if the version constraint is met.
#
# RTX 5060 (sm_120) note:
#   HuggingFace models fall back to CPU automatically (see ai_service.py).
#   Ollama uses the GPU natively regardless - no change needed.
# =========================================================================

--extra-index-url https://download.pytorch.org/whl/cu121


# Web Framework
# =============
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
slowapi>=0.1.9,<0.2.0              # Rate limiting - REQUIRED (dependencies/rate_limit.py)


# Database
# ========
motor>=3.3.0,<4.0.0                # Async MongoDB driver (Motor 3.x API)
pymongo>=4.7.0,<5.0.0              # Sync driver; motor depends on pymongo 4.x


# Data Validation & Settings
# ==========================
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
email-validator>=2.1.0,<3.0.0      # EmailStr field support in pydantic


# Authentication & Security
# =========================
bcrypt>=4.1.0,<5.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0


# Utilities
# =========
python-dotenv>=1.0.0,<2.0.0
cachetools>=5.3.0,<6.0.0           # TTLCache for JWT user lookups - REQUIRED (dependencies/auth.py)
pytz>=2024.1                        # Timezone support (Asia/Karachi display)
httpx>=0.27.0,<1.0.0               # Async HTTP client - Ollama API calls + test client


# AI/ML - HuggingFace Inference
# ==============================
# torch: the CUDA 12.1 wheel is fetched from --extra-index-url above.
# This wheel also runs on CPU - GPU is used automatically when available.
# Models loaded: distilbert-base-uncased-finetuned-sst-2-english (268 MB)
#                SamLowe/roberta-base-go_emotions (499 MB)
# Both are text-only - torchvision/torchaudio are NOT needed.
#
torch>=2.3.0,<2.7.0
transformers>=4.40.0,<4.52.0       # DistilBERT + RoBERTa model loading
accelerate>=0.30.0,<2.0.0          # Mixed-precision & device-map for model loading
sentencepiece>=0.1.99,<0.3.0       # Fast tokenizer backend (transformers dependency)


# AI/ML - LoRA Finetuning
# ========================
# These are only needed when running the finetuning pipeline scripts.
# They do NOT affect the running FastAPI app.
#
peft>=0.10.0,<0.16.0               # LoRA / PEFT adapters (finetune.py)
datasets>=2.19.0,<3.6.0            # HuggingFace dataset loader (download_datasets.py)
tqdm>=4.66.0,<5.0.0                # Progress bars (preprocess.py, finetune.py)
rouge-score>=0.1.2,<0.2.0          # ROUGE-L evaluation metric (evaluate.py)
nltk>=3.8.0,<4.0.0                 # BLEU evaluation metric (evaluate.py)


# LLM Chat - Ollama
# =================
# model_service.py calls Ollama's HTTP API directly via httpx (no SDK needed).
# The ollama package below is for scripting convenience (e.g. ollama.pull()).
# Run 'ollama pull llama3.1:8b' before starting the backend.
#
ollama>=0.3.0,<1.0.0


# Testing
# =======
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.21.0,<1.0.0
